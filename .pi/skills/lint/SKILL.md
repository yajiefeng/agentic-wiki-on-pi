# Lint

对 wiki 进行健康检查。

## 步骤

1. 读取 wiki/index.md 和最近的 wiki/log.md 条目
2. 扫描 wiki/ 下所有页面，检查：
   - 孤立页面（没有被其他页面引用）
   - 断裂链接（引用了不存在的页面）
   - 过时信息（被更新的源覆盖但未标注）
   - 缺失页面（被多次提及但没有独立页面的概念/实体）
   - frontmatter 不完整
3. 输出问题清单，按优先级排序
4. 问用户是否要自动修复（创建缺失页面、补充交叉引用等）
